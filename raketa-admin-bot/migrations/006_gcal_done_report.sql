-- Отметка «об этой работе владельцу уже отчитался» (неделя наблюдения).
-- Применяется вручную только при первой установке; скрипт обновления
-- прогоняет все файлы migrations/*.sql сам:
--   psql "$ADMINBOT_DB_DSN" -f migrations/006_gcal_done_report.sql
--
-- Зачем нужна. Отчёт о сделанной работе уходил на каждом проходе, где запись
-- была «готова» или «жду автосделку», а проходов по одной записи бывает
-- несколько подряд — владелец получал по три почти одинаковых сообщения.
-- Отметка работает так же, как question_msg_id у карточки-вопроса: номер
-- отправленного сообщения означает «отчёт доставлен, второй раз не пишем».

ALTER TABLE adminbot.gcal_events
    ADD COLUMN IF NOT EXISTS done_msg_id bigint;
