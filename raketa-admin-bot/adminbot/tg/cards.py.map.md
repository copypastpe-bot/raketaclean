# Карта функций: adminbot/tg/cards.py

Заведена 2026-09-21 (задача 7 ТЗ `2026-09-21-evening-summary-rework.md`: файл
перешёл порог 500 строк — вырос с 480 до 552 за задачи 3–5, 9 того же ТЗ:
хвосты разнесены по категориям, новый текст вечерней сводки, ссылки на сделки,
строка о сбое календарного прохода).
Строк в файле: 552.

Правило файла: весь модуль — чистые функции без сети и базы (см. докстринг
файла), поэтому каждую формулировку можно проверить тестом без БД. Тексты для
владельца — не термины из кода: «проведено», «ждут вашего ответа», а не
статусы из базы.

## Общее

| Строка | Имя | Назначение |
|---|---|---|
| 26–33 | `CHOICE_PREFIX`, `CLEANING_CHOICE_PREFIX`, `PREFIX_BY_KIND`, `BACKLOG_GO`, `BACKLOG_HOLD` (константы) | приставки callback-данных карточки-вопроса и кнопок предпросмотра хвоста |
| 37 | `REHEARSAL_PREFIX` (константа) | пометка «это репетиция» — общая для заказов, уборок, ковров и календаря |
| 40 | `mark_rehearsal` | добавить пометку репетиции в первую строку текста, если `dry_run` |
| 45–55 | `REASON_TEXTS`, `DEFAULT_REASON` (константы) | почему робот спрашивает — словами владельца, по коду причины |
| 58–68 | `ACTION_WORDS` (константа) | что робот сделает — словами владельца, для предпросмотра хвоста |

## Карточка-вопрос (заказы и уборки)

| Строка | Функция | Назначение |
|---|---|---|
| 73 | `question_card` | текст и кнопки вопроса по одной работе: что за работа, между чем выбирать |
| 100 | `parse_choice` | разобрать нажатие кнопки карточки-вопроса → (номер работы, выбор, id сделки или `None`) |
| 120 | `_option_button` | кнопка одного варианта-сделки |
| 126 | `_option_label` | подпись варианта: дата или номер сделки, сумма |
| 135 | `_choice` | собрать callback-данные `{prefix}:{order_id}:{value}` |
| 139 | `_order_line` | одна строка «что за работа»: номер, имя, телефон, сумма, дата заказа |

## Карточка «сделка без адреса» (ТЗ 2026-09-16, задача 7)

| Строка | Имя | Назначение |
|---|---|---|
| 155–156 | `ADDR_PREFIX`, `CLEANING_ADDR_PREFIX` (константы) | свои приставки callback-данных — не пересекаются с карточкой-вопросом |
| 159 | `address_missing_card` | текст и кнопки напоминания: сделка заведена, адреса в ней нет |
| 181 | `parse_address_choice` | разобрать нажатие на этой карточке → (номер работы, `filled`/`mute`) |

## Вечерняя сводка (ТЗ 2026-09-21-evening-summary-rework.md)

Числа и хвосты — две независимые части, которые строит `summary_text`
раздельными функциями (`_numbers_block`, `_tails_block`) и только склеивает —
задача 4: когда потоки переедут на шину оповещений, резать нужно будет по
этому шву. Заказы и уборки в хвостах идут одним общим списком без разделения
по потоку (задача 3, решение владельца 21.09), а в числах — раздельно
(задача 2/4).

| Строка | Имя | Назначение |
|---|---|---|
| 200–205 | `_TAIL_CATEGORIES` (константа) | четыре категории хвостов и их подписи, в порядке утверждённого макета |
| 208 | `TAIL_CAP` (константа) | не больше позиций в категории, дальше «…и ещё N» (умолчание координатора 21.09) |
| 214 | `CALENDAR_FAILURE_LINE` (константа) | задача 9: текст строки о сбое календарного прохода в хвостах |
| 217 | `summary_text` | собрать сообщение вечерней сводки целиком: заголовок → числа → уборки → хвосты |
| 260 | `_numbers_block` | четыре числа «что сделал робот» за сутки (задачи 2, 4, 8); «Событий не было.», если все нули |
| 276 | `_tails_block` | блок хвостов: строка о сбое календаря (задача 9) + четыре категории заказов и уборок вместе |
| 296 | `_tail_rows` | строки одной категории: номер, телефон, дата, ссылка на сделку; у сбоя ещё текст ошибки |
| 330 | `_lead_ref` | номер сделки в строке хвоста — `#<номер>`, а с известным `base_url` кликабельная ссылка (задача 5) |

## Предпросмотр и отчёт по хвосту заказов (`backlog`)

| Строка | Функция | Назначение |
|---|---|---|
| 342 | `preview_card` | текст и кнопки предпросмотра хвоста — репетиция перед боевым прогоном |
| 364 | `live_report_text` | отчёт после боевого прогона хвоста |
| 375 | `_action_words` | одно действие робота словами владельца, с номером сделки, если он уже есть |
| 384 | `_status_words` | статус работы словами владельца |
| 395 | `_orders_word` | склонение слова «заказ» по числу |
| 403 | `_as_date` | привести значение даты варианта-сделки к `date` |
| 414–417 | `PATH_BY_PIPELINE` (константа) | воронка амо → путь заказа (A/B) |

## Ковры от партнёра

| Строка | Функция | Назначение |
|---|---|---|
| 422 | `CARPET_PREFIX` (константа) | приставка callback-данных ковровой карточки-вопроса |
| 424–431 | `CARPET_REASONS`, `CARPET_DEFAULT_REASON` (константы) | почему робот спрашивает по ковровой строке — словами владельца |
| 434 | `carpet_question_card` | текст и кнопки вопроса по строке отчёта партнёра |
| 473 | `parse_carpet_choice` | разобрать нажатие ковровой карточки-вопроса |
| 487 | `carpet_report_text` | итог разбора одного письма партнёра — сколько проведено, сколько ждёт, сколько ошибок |
| 509 | `carpet_held_text` | письмо партнёра отложено — почему и что делать дальше |
| 527 | `_carpet_choice` | собрать callback-данные ковровой карточки |

## Сообщения о сделанной работе (наблюдение с 2026-08-27)

| Строка | Имя | Назначение |
|---|---|---|
| 534–539 | `PATH_WORDS` (константа) | путь заказа (A/B/C/`done`) → что это значило на деле, словами владельца |
| 542 | `order_done_text` | сообщение о проведённой работе — по горячим следам, с ссылкой на сделку |
