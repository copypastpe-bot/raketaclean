# AGENT_STATE

project: raketaclean
last_updated: 2026-09-19
updated_by: claude
status: active
confidence: high

## Purpose

Один репозиторий, три службы. Корень — рабочий бот клининговой компании (заказы, лояльность, зарплата, касса, уведомления, админка, обмен с amoCRM и письма клиенту). `raketa-admin-bot/` — админ-бот только для владельца: amo_sync, ковры, два календаря, автозвонок, разбор удалённых заказов. `raketa-notify/` — служба оповещений (шина), на сервер ещё не поставлена. Базу рабочего бота админ-бот читает только на чтение, в amoCRM пишет своей интеграцией.

## Current State

**Прод не менялся с 18.09:** оба бота на `ddf4900`, всё новое выключено, пульс молчит. Служба `raketa-notify` на сервер не ставилась — установку делает владелец по `raketa-notify/docs/runbook.md` (нужны sudo, секреты и роль `notify`).

**Ветка `feature/notifications-review-fixes`** (от `main` `59d9bae`): **не слита и не запушена**, решение о пуше за владельцем. Код ботов не тронут ни строкой — все правки внутри `raketa-notify/` и документов.

**Сделано по ТЗ `docs/plans/2026-09-19-notifications-review-fixes.md`** (шесть задач; четыре отложенных замечания ревью 18.09 плюс два новых решения владельца): один цикл проверок вместо двух (два цикла затирали друг другу замер очереди); уровень поломки читается из справочника и меняется на лету; ночная пауза 00:00-08:00 МСК для красных повторов; тревога только по поломке, продержавшейся три прохода; маскировка адресов и схлопывание повторов. **Ревью ветки нашло пять дефектов сверх ТЗ, все починены.**

**Тесты:** служба 108 (было 71), рабочий бот 356, админ-бот 911 плюс 9 пропущено — код ботов не менялся, прогон контрольный.

**Инфраструктура, 19.09 (в git не отражается, живёт на серверах):** прокси Telegram усилен — `MaxClients` 60 → 200, `Timeout` 600 → 120; прежний образ и копия конфига оставлены для отката. SSH сервера ботов закрыт: вход только по ключу, `root` паролем запрещён.

## Pending

**Решение владельца:** слить ветку в `main` и выложить, либо держать до установки службы.

**Установка службы** — за владельцем: `raketa-notify/docs/runbook.md`, вписать на сервере строку подключения, токен My_admin, номер технического чата, адреса и прокси Telegram.

**Второе ТЗ, после замера:** переезд существующих потоков по справочнику и выключения (SMS-контур, маркетинговый чат, устаревшие процессы amoCRM).

**Бриф на пять задач** — `docs/plans/2026-09-18-brief-notifications-backlog.md`; владелец продолжит его в понедельник.

**В парке после 19.09:** сторож прокси с автоперезапуском, вторая труба на новом сервере (владелец его планирует), `fail2ban`, сужение прав `admin`.

## Known Limitations

- **Обрывы Telegram 19.09 не объяснены:** волна 19:14-19:51 МСК у трёх ботов разом. Прокси усилен как страховка, но причина (прокси или канал РФ→NL) не доказана. Если повторится — замер с двух сторон одновременно.
- **Прокси — HTTP `tinyproxy`, не SOCKS5**, с паролем и белым списком адресов. Проверять только HTTP CONNECT и учёткой ботов; проба не тем протоколом выглядит как поломка. Исходники на Contabo в `/home/deploy/telegram-proxy/`, конфиг вшит в образ.
- **Прокси вне РФ — общая точка отказа трёх ботов**; если он ляжет, служба не сообщит и о нём самом.
- **У `admin` на timeweb sudo без пароля на `tee`, `rm`, `ln`, `mkdir`** — фактически права root; `reload ssh` при этом требует пароля владельца.
- **Ночная пауза считается по `Europe/Moscow`** из системной базы часовых поясов — на сервере нужен `tzdata`.
- **Счётчик проходов и пометка о серой поломке живут в памяти процесса:** после перезапуска службы поломка подтверждается заново.
- **Уровень записи журнала переходник берёт из текста строки**, а не из метки systemd — предположение исполнителя, на живом journald не проверено.
- **Курсор журнала не переживает перезапуск службы**: после рестарта читается с текущего конца.
- **`notifications/outbox.py:626`** кладёт в журнал весь payload вебхука с телефоном клиента; защита — маскировка в переходнике.
- **rtk-обёртка врёт по pytest** («No tests collected» при прошедших тестах). Настоящий прогон — через `rtk proxy ... -m pytest`.
- **Тестовая база службы у координатора отдельная** (`notify_test_coord`): параллельные исполнители дерутся за пересоздание схемы в `notify_test`.
- **Папка `robot_amo` — боевой вход**: файл партнёра до 100 строк робот проведёт и заведёт сделки.
- **Файлы, положенные на сервер руками, ломают штатное обновление** (`git pull` не перезапишет untracked-файл).
- **Бутстрап схем создаёт таблицы с нуля, но не мигрирует существующие** — миграции накатывать руками ролью `bot` до рестарта.
- Деплой ручной, SSH `admin@91.200.150.68`. Рабочий бот: `git pull --ff-only`, миграции руками, рестарт. Админ-бот — `rsync`, затем `sudo raketa-admin-bot-update`.
- Тесты: корень — `.venv-wahelp/bin/python -m pytest tests/ -q` с `raketaclean_test`; админ-бот — из его каталога с `adminbot_test`; служба — из `raketa-notify/` с `notify_test`.
