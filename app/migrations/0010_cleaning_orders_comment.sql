-- Уборки переходят на то же устройство адреса, что и химчистка (0008, 0009):
-- адрес — из amoCRM (фоновый проход бэкфилла), а не с ручного ввода бригадира.
-- NOT NULL снимаем — иначе уборка перестанет проводиться, как только исчезнет
-- вопрос про адрес. Комментарий — новое необязательное поле, его пишет человек,
-- в amoCRM не уходит (решение владельца 17.09).
--
-- ТЗ 2026-09-17 «адреса в клининге (уборки)», задача 1.

ALTER TABLE cleaning_orders ALTER COLUMN address DROP NOT NULL;
ALTER TABLE cleaning_orders ADD COLUMN IF NOT EXISTS comment text;
