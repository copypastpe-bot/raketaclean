# Бриф: отложенные задачи из разбора оповещений

дата: 2026-09-18
кому: параллельной сессии владельца
статус: постановка, не ТЗ. По каждой задаче сперва выясняются факты, потом решение владельца,
и только потом пишется ТЗ.

Основная работа (шина оповещений) идёт отдельным ТЗ и в этот бриф не входит.
Полный разбор, из которого выросли эти задачи: `docs/recon/2026-09-18-service-messaging-map.md`.

---

## Общий контекст: что уже решено 18.09

**Четыре адреса вместо сегодняшней мешанины:**

1. Рабочие чаты (заказы, деньги, касса уборок, карта Жени) — не трогаем; вечерние сводки
   по работе конторы остаются там же.
2. Операционная лента — нынешний чат логов, со звуком.
3. Технический журнал — новый чат, без звука, каждое сообщение с `#тегом`.
4. Тревога — два личных бота владельца, больше их никто не видит:
   **My_assistant** (дела конторы; это уже существующий бот админ-бота, где владелец отвечает
   на вопросы по спорным сделкам и удалениям — эту работу не менять) и
   **My_admin** (железо и серверы, новый).

**Уровни срочности:** красный — повтор каждые 10 минут, ночью тоже, пока владелец не нажмёт
«сел разбираться» (кнопка молчит час и возвращается, если поломка жива; отбой робот даёт сам,
проверив); жёлтый — редкое напоминание с потолком, можно отложить; серый — в ленту или журнал,
не дёргает.

**Как реализуется:** независимая служба-почтальон. Боты кладут событие в почтовый ящик в базе,
служба разбирает, шлёт по адресам, ведёт инциденты и сторожит живость. Куда идёт событие,
каким уровнем и с каким тегом — решает справочник маршрутов, а не код.

---

## Задача 1. Устаревшие процессы amoCRM (приоритет А)

**Что сейчас.** В личку админов идут три потока: новая заявка из amoCRM, новый лид и
неразобранное, клиенту не ответили вовремя.

**Слова владельца (18.09):** «не то чтобы мусор из сообщений, а просто ненужные вещи, сами БП
устарели, события обрабатываются иначе уже». То есть чинить надо не тексты, а механику.

**Что выяснить:**
- чем именно заменились эти процессы и кто сегодня реально обрабатывает заявки;
- какие из трёх потоков ещё нужны, а какие дублируют новую механику;
- если поток нужен — кому он адресован теперь (владельцу, менеджеру, в ленту).

**Что нужно от владельца:** решение по каждому потоку — оставить, переделать или выключить.

**Источник фактов:** замер (задача 3) плюс рассказ владельца. Разбор по коду уже сделан,
см. карту.

---

## Задача 2. Автозвонок по отклику на промо и по оценке (приоритет B)

**Что сейчас.** Клиент отвечает «1» на промо, интересуется акцией или ставит оценку заказу —
админам приходит сообщение, дальше звонят руками.

**Что хочет владелец:** звонок автоматизировать.

**Что выяснить:**
- как устроен нынешний автозвонок по заявке с сайта (он в бою с 02.09) и можно ли его
  переиспользовать целиком;
- утверждённые правила окна звонка и тегов — их надо поднять из готовых решений, а не
  придумывать заново;
- что делать при недозвоне и нужна ли под такой звонок сделка в CRM.

**Осторожно:** автозвонок звонит на мобильный менеджера напрямую. Любая ошибка здесь видна
живому человеку сразу.

---

## Задача 3. Замер объёма технических сообщений — ЗАКРЫТА 21.09

**Результат:** считать по журналам за сутки оказалось достаточно, лезть на сервер
со скриптом не понадобилось. Рабочий бот — 30 записей уровня «предупреждение» и
«ошибка» (19 `crm.wahelp_dispatcher`, 6 `notifications.worker`, 5 `__main__`),
админ-бот — 16 (10 `aiogram.dispatcher`, 4 `adminbot.amo.client`,
2 `adminbot.tg.outbox`). Итого около 46 в сутки, примерно две в час: технический
чат таким потоком не зальёт, правила схлопывания крутить не нужно.

Ниже — исходная постановка, оставлена для истории.

### Исходная постановка

**Зачем.** Технический журнал включается с нуля, а мест, которые в него посыплются, порядка 170.
Без числа непонятно, получится читаемая лента или водопад, который владелец выключит через день.

**Что сделать:** снять журналы обеих служб за неделю и посчитать: сколько записей уровня
«ошибка» и «предупреждение» в сутки, как они распределены по источникам, какие приходят пачками.

**Ограничения:** на сервер — разово и скриптом, вывод обрезать до полезной части.
Владелец разрешил замер 18.09, но просил не тратить лимиты зря.

**Результат:** число в сутки по каждому источнику. На нём строятся правила схлопывания повторов
(«×15 за десять минут» одной строкой) и список тегов.

---

## Задача 4. SMS-контур — ЗАКРЫТА 21.09, код удалён

**Почему встал.** Подписка на события в кабинете OnlinePBX была в состоянии ошибки
(«горела красным», слова владельца 21.09) и событий не присылала. Последнее событие —
**5 мая 2026**, дальше тишина. В журнале рабочего бота за три месяца ноль запросов
и ноль отказов по IP или токену: запросов не было вовсе. Сам бот был исправен, учётка
телефонии жива — автозвонок админ-бота ходит в тот же OnlinePBX и работает. Подписку
владелец удалил 21.09, разбираться в причине ошибки не стал.

**Сколько прожил.** С 10.03 по 05.05.2026. За два месяца 221 входящий звонок попал
в журнал, SMS реально отправлена **6 раз**: 161 предложение осталось без ответа админа,
33 отсеяно коротким разговором, 15 отказов, 6 ошибок.

**Где ещё была отправка SMS.** Нигде: единственный путь — sms.ru через подтверждение
админа. Канал SMS в Wahelp в настройках числился (`WAHELP_CLIENTS_SMS_*`), но кодом
не использовался.

**Решение владельца:** вырезать код целиком, а не заводить выключатель — своего
выключателя у контура не было, и заводить его ради выключения смысла нет.

**Что сделано** (коммиты `4a37575`, `6deca3e`, выкачено 21.09): удалены приём
`/onlinepbx/webhook` с проверками IP и токена, обработчик звонка, предложение админам
и обработка решения, отправка через sms.ru, слежение за балансом, создание таблицы,
семь настроек, упоминания в README и `project.md`; карта функций `bot.py` пересчитана.
Из `switches.env` на сервере убраны `ONLINEPBX_ALLOWED_IPS` и `ONLINEPBX_SMS_TEXT`.

**Оставлено намеренно:** таблица `onlinepbx_sms_requests` с 221 записью (архив звонков),
ключ `SMSRU_API_ID` в `.env` — «вдруг пригодится для других фич», и
`scripts/download_onlinepbx_calls.py` вместе с `ONLINEPBX_API_BASE`/`ONLINEPBX_API_TOKEN`:
это отдельный инструмент выгрузки записей разговоров, к SMS отношения не имеет.

Ниже — исходная постановка, оставлена для истории.

### Исходная постановка

**Что сейчас.** Два сообщения админам: запрос «отправить SMS?» после звонка клиента и
предупреждение о низком балансе sms.ru. Владелец: «фича не работает, не используется,
просто перестала».

**Что выяснить:**
- когда и почему перестала работать — телефония, баланс, доступ или что-то ещё;
- используется ли отправка SMS где-то ещё, кроме этих двух мест (найти все, не полагаться
  на список).

**Решение владельца:** выключить весь контур целиком, а не два сообщения из него.
Способ — за владельцем: рубильником (рекомендация координатора) или закомментировать код.

---

## Задача 5. Переделка вечерней сводки

**Что сейчас.** Сводка в 21:00 присылает накопленное с момента включения процесса.
Владелец: «тупая простыня, хз зачем».

**Уже решено:** события за день уходят в операционную ленту; висяки — в My_assistant,
с логикой напоминания, чтобы владелец не «посмотрел и забыл».

**Что выяснить и решить:**
- что считается висяком — перечень состояний;
- за какой период он собирается;
- когда напоминать, сколько раз и что считается закрытием висяка.

---

## Задача 6. Панель управления в вебе (направление, не сейчас)

**Слова владельца (21.09):** «на весь этот огромный ворох функций надо будет сделать
панель управления в веб. Не сейчас, просто это уже нужно, слишком много всего».

**Откуда выросло.** Управление размазано по файлам и командам на сервере: выключатели
служб, справочник маршрутов и уровней, пульс ботов, ковры, автозвонок, календари.
Каждая настройка — это ssh, файл и перезапуск. Разделение секретов и выключателей
(21.09) — первый шаг в ту же сторону: состояние, которым управляют, вынесено из
кода и из секретов.

**Что выяснить, когда дойдут руки:**
- какие именно ручки нужны владельцу в вебе, а какие остаются агенту и терминалу;
- где панель живёт (тот же сервер, отдельный) и как закрывается от чужих;
- берём ли готовое (админка поверх базы) или пишем своё;
- что панель показывает кроме ручек: состояние служб, что сейчас сломано, очередь.

**Не начинать без разбора с владельцем:** это отдельный проект, а не задача
внутри оповещений.

## Задача 7. Досылка в WhatsApp — ЗАКРЫТА 21.09, функция удалена

**Решение владельца 21.09:** «Выключить. Не прочитал, прочитает потом. Долбить во все
мессенджеры — не надо». Способ выбран тот же день: не выключатель, а удаление кода —
«вырезать совсем и ладно, мусор, вводит в заблуждение».

**Что сделано.** Досылка вырезана целиком, коммит `3de9bac`, выкачено 21.09:
планирование и отмена в `crm/wahelp_dispatcher.py`, экспорт из `crm`, подключение
в `notifications/webhook.py`, хвост в `apply_provider_status_update` и два её лишних
параметра, тест, выключатель `WA_FOLLOWUP_DISABLED` из образца настроек, упоминание
в `project.md`. Обновление статусов доставки и каналов клиента не тронуто.

**Что выяснилось по дороге и стоит помнить:**
- работала она только в одну сторону — Telegram доставлен, но не прочитан → дубль
  в WhatsApp; обратного направления не было никогда;
- дубль уходил напрямую по номеру, **минуя** выбор канала, дневной лимит и проверку
  живости канала: клиенту без WhatsApp попытка всё равно делалась;
- **ошибка исходной постановки:** у очереди `notification_outbox` протухания по сроку
  нет. Есть откладывание (`scheduled_at`), повторы (`attempts`, до пяти) и отмена.
  Протухание пришлось бы писать заново — это меняло цену переделки;
- десять месяцев функция вообще не работала, и этого никто не заметил. Это и решило
  вопрос: чинить то, чьё отсутствие незаметно, дороже, чем убрать.

Ниже — исходная постановка, оставлена для истории.

### Исходная постановка

**Что сейчас.** Клиент получил сообщение и не открыл его — через сутки бот должен
продублировать текст в WhatsApp. Напоминание «написать через 24 часа» бот держит
**в своей памяти** (`_followup_tasks` в `crm/wahelp_dispatcher.py`, задача с
`asyncio.sleep`), а не в базе. Любой перезапуск стирает все такие напоминания.

**Факты, снятые 21.09:**
- бот перезапускался **6 раз за неделю** — примерно раз в сутки при задержке ровно
  в сутки, то есть теряется заметная часть досылок;
- досылка планируется из `notifications/webhook.py` по событию «доставлено»
  и отменяется, когда клиент прочитал (`cancel_followup_for_client`);
- за то время, что хранил журнал (с 7 июля), досылка доходила до отправки 401 раз —
  и все 401 падали по другому дефекту (импорт), он починен 21.09, коммит `c1deddf`;
- образец правильного устройства уже есть в проекте: очередь сообщений клиентам
  `public.notification_outbox` переживает перезапуск, её разбирает
  `notifications/worker.py`.

**Что выяснить:**
- сколько досылок теряется на самом деле: сравнить «запланировано» и «сработало»
  за неделю после починки импорта;
- переиспользовать `notification_outbox` (у неё уже есть повторы, протухание и
  контуры) или завести отдельную таблицу под досылку;
- что делать с досылками, запланированными до перезапуска: поднимать при старте
  из базы или считать протухшими после какого-то срока;
- нужен ли досылке собственный выключатель (сегодня есть `WA_FOLLOWUP_DISABLED`).

**Что нужно от владельца:** решение, переиспользуем существующую очередь или заводим
свою, и какой срок считать протуханием досылки.

**Осторожно:** после починки 21.09 досылки реально уходят клиентам — около 16 в сутки.
Любая правка здесь меняет то, что видят живые клиенты.

## Задача 8. Ожидание автосделки: таймер никогда не срабатывает (дефект, найден 21.09)

**Что должно быть.** Запись календаря ждёт, пока salesbot заведёт сделку в воронке
реализации. Не дождался за `AMO_SYNC_SALESBOT_WAIT_SEC` (на сервере 600 секунд) —
робот спрашивает владельца «сейлзбот не создал автосделку».

**Что на самом деле.** Вопрос не приходит никогда. Отсчёт идёт от `updated_at` записи
(`adminbot/gcal/engine.py`, шаг ожидания salesbot), а сам шаг при каждом проходе
пишет `status="waiting_salesbot"` — и `update_calendar_link` ставит `updated_at = now()`.
Наблюдатель ходит раз в 60 секунд, пока есть незаконченные записи
(`QUICK_RETRY_SEC`, `adminbot/gcal/watcher.py`), поэтому 600 секунд не набегают.

**Доказательство из базы (21.09).** Единственная запись в `waiting_salesbot`:
заказ на 20.09, ковры на дому, район приокский, `path=C`, `primary_lead_id=31665057`,
`real_lead_id` пуст. Создана 20.09 11:32, `updated_at` 21.09 19:10 — обновлена
за минуту до замера. Висит больше 31 часа, владельцу не сообщено.

**Решение владельца 21.09:** зафиксировать, чинить вместе с темой адресов.

---

## Задача 9. Удалённая запись календаря закрывает не ту сделку (дефект, со слов владельца 21.09)

**Слова владельца:** удаление записи в календаре должно закрывать сделку **не в воронке
реализации, а в воронке первичной обработки**, и вместе с ней — её подчинённую сделку.

**Статус:** только зафиксировано. По коду не проверялось — владелец просил не ходить
проверять сейчас. Перед работой поднять факты: какую сделку робот закрывает сегодня,
что считается подчинённой и как они связаны.

---

## Чего не делаем (решения владельца 18.09)

- **Напоминания мастерам и бригадирам** — в парк: проблем с забыванием работы нет.
- **Дежурный среди админов** — не нужен: админ один, владелец его контролирует.
