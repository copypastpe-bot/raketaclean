aiogram==3.22.0
asyncpg==0.30.0
aiohttp==3.12.15

# aiogram требует эти пакеты для ЛЮБОГО прокси, не только SOCKS.
# Без них служба падает при старте, если TELEGRAM_PROXY_URL задан
# (проверено координатором 18.09, тот же набор у админ-бота).
aiohttp-socks==0.12.0
python-socks==3.0.0
